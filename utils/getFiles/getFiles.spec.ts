import test from 'ava';
import { getFiles } from './getFiles';

const path = './__mocks__';

// test.todo('works as expected',
// async (t) => {
//   const files = await getFiles([path]);

//   console.log(files);
//   t.pass();
// });
